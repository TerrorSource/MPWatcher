services:
  mpwatcher:
    build: .
    container_name: mpwatcher
    ports:
      - "8000:8000"
    volumes:
      - ./config:/config
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
